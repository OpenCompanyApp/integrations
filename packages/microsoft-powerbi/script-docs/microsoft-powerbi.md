# Legacy Microsoft Power BI JavaScript Reference

This package is deprecated. Use the canonical `powerbi` namespace from
`opencompanyapp/integration-microsoft-power-bi`.

The maintained tools are documented in
`packages/microsoft-power-bi/script-docs/powerbi.md`. This legacy package no longer
registers a separate `microsoft_powerbi` namespace because that created duplicate
catalog entries for the same service family.
