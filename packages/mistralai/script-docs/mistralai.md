# MistralAI Legacy Package

This package is a compatibility alias for the canonical `mistral` integration.

Use the `mistral` JavaScript namespace and docs in `packages/mistral/script-docs/mistral.md`.
