# Legacy Notion Lua Reference

This package is deprecated. Use the canonical `notion` namespace from
`opencompanyapp/integration-notion`.

The maintained tools are documented in `packages/notion/lua-docs/notion.md`.
This legacy package no longer registers a separate `notion2` namespace because
that created duplicate catalog entries for the same service family.
