# Legacy Retell Lua Reference

This package is deprecated. Use the canonical `retell-ai` namespace from
`opencompanyapp/integration-retell-ai`.

The maintained tools are documented in
`packages/retell-ai/lua-docs/retell-ai.md`. This legacy package no longer
registers a separate `retell` namespace because that created duplicate catalog
entries for the same service family.
