# Teams Legacy Package

This package is a compatibility alias for the canonical `microsoft-teams` integration.

Use the `microsoft-teams` Ruby namespace and docs in `packages/microsoft-teams/script-docs/microsoft-teams.md`.
